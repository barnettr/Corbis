namespace Corbis.Web.Entities
{
    /// <summary>
    /// Controls the available selections for Items per page for search results,
    /// lightboxes, order history, etc.
    /// </summary>
    public enum ItemsPerPage
    {
        items50 = 50,
        items100 = 100,
        items200 = 200
    }
}